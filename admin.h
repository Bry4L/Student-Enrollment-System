#ifndef ADMIN_H
#define ADMIN_H

#include "authentication.h"
#include "student.h"
#include "instructor.h"

void adminMenu(UserList& userList, StudentList& studentList, InstructorList& instructorList); // reference to the object contain list of all data //allow to perform

#endif
